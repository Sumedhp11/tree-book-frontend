/* eslint-disable react/prop-types */

import { Dialog, DialogContent, DialogTrigger } from '../ui/dialog'

const ModalForm = ({ children, DialogBodyContent, open, setOpen }) => {
    return (

        


    )
}

export default ModalForm